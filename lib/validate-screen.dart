import 'package:flutter/material.dart';
class ValSec extends StatelessWidget {
  const ValSec({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.grey,
        title: Text('Screen after validation'),
      ),
    );
  }
}
